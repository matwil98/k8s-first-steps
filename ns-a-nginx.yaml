apiVersion: v1
kind: Pod
metadata:
  name: nginx-01
  namespace: ns-a
  labels:
    app: nginx
spec:
  containers:
  - name: nginx-01
    image: nginx
    ports:
    - containerPort: 80
    - containerPort: 443
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
